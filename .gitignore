node_modules

.env
.env.local
.DS_Store
.cache
.output
.nitro
.tanstack

/dist/
/dist-ssr/

# Vercel Build Output API bundle (produced by build-vercel.sh at go-live).
/.vercel/

# Runtime server log written on start.
/.run/

# Generated by the TanStack Start router plugin on dev/build.
src/routeTree.gen.ts

# Webhook fallback secret (whsec pinned for stale-runtime env immunity); never commit.
src/data/webhook-secret.json
# First-party analytics event log (runtime state); never commit.
/data/
